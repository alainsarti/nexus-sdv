apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-api-sampler.fullname" . }}
  labels:
    {{- include "data-api-sampler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-api-sampler.selectorLabels" . | nindent 4 }}